<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Server-side Validation Demo</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <main class="container">
      <section class="card">
        <h1>Register</h1>
        <p class="subtitle">
          Server-side validation powered by Express and express-validator.
        </p>
        <form action="/register" method="POST" novalidate>
          <div class="field">
            <label for="fullName">Full Name</label>
            <input
              type="text"
              id="fullName"
              name="fullName"
              value="<%= data.fullName ? data.fullName : '' %>"
              aria-describedby="fullName-error"
              required
            />
            <% if (errors.fullName) { %>
            <p id="fullName-error" class="error"><%= errors.fullName.msg %></p>
            <% } %>
          </div>

          <div class="field">
            <label for="email">Email</label>
            <input
              type="email"
              id="email"
              name="email"
              value="<%= data.email ? data.email : '' %>"
              aria-describedby="email-error"
              required
            />
            <% if (errors.email) { %>
            <p id="email-error" class="error"><%= errors.email.msg %></p>
            <% } %>
          </div>

          <div class="field">
            <label for="password">Password</label>
            <input
              type="password"
              id="password"
              name="password"
              aria-describedby="password-error"
              required
            />
            <% if (errors.password) { %>
            <p id="password-error" class="error"><%= errors.password.msg %></p>
            <% } %>
          </div>

          <div class="field">
            <label for="confirmPassword">Confirm Password</label>
            <input
              type="password"
              id="confirmPassword"
              name="confirmPassword"
              aria-describedby="confirmPassword-error"
              required
            />
            <% if (errors.confirmPassword) { %>
            <p id="confirmPassword-error" class="error">
              <%= errors.confirmPassword.msg %>
            </p>
            <% } %>
          </div>

          <div class="field">
            <label for="age">Age</label>
            <input
              type="number"
              id="age"
              name="age"
              value="<%= data.age ? data.age : '' %>"
              aria-describedby="age-error"
              min="13"
              max="120"
            />
            <% if (errors.age) { %>
            <p id="age-error" class="error"><%= errors.age.msg %></p>
            <% } %>
          </div>

          <div class="field field-checkbox">
            <input
              type="checkbox"
              id="terms"
              name="terms"
              <%= data.terms ? 'checked' : '' %>
              aria-describedby="terms-error"
            />
            <label for="terms">I agree to the terms and conditions</label>
          </div>
          <% if (errors.terms) { %>
          <p id="terms-error" class="error"><%= errors.terms.msg %></p>
          <% } %>

          <button type="submit" class="btn">Create Account</button>
        </form>
      </section>
    </main>
  </body>
</html>

